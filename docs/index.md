# Intro

Welcome to the documentation site dedicated to the `rou2exOS` (or just `r2`) kernel for the x86_64 CPU architecture.

![memento](/r2-memento-hello-login.png)

Fig. 1: External userspace program demo called `MEMENTO.ELF`. Login window in the foreground, a wallpaper in the background.

## Story

The original project called "RoureXOS" (or just the version 1) has been a solid foundation of its next iteration --- `r2`. 

...